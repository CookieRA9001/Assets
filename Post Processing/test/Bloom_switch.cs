using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering;

public class Bloom_switch : MonoBehaviour
{
    void Start()
    {
        /*VolumeProfile vol = GetComponent<Volume>().profile;
        UnityEngine.Rendering.Universal.Bloom bloom;
        vol.TryGet(out bloom);
        
        if(true){
            bloom.intensity.Override(2);
        }
        else{
            bloom.intensity.Override(0);
        }*/
    }
}
